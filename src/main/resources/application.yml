server:
    port: 9093

Spring:
   profile:
      active: local
   application:
            name: USER-SERVICE
   datasource:
       driver-class-name: com.postgresql.driver
   jpa:
     hibernate:
        ddl-auto: update
     show-sql: true
     database-platform: org.hibernate.dialect.PostgresSQLDialect


